import { Client, RunParams, AttestationResult, SDKConfig, AttestationQuoteData, AttestationVerificationResult, DataAvailabilityResult, CelestiaGetParams, CelestiaGetResult } from './types';
/**
 * Main SDK client implementation for executing services with attestation verification.
 *
 * This class orchestrates HTTP requests to the Livy proxy API and wraps responses
 * in AttestationResult objects that provide verification capabilities.
 */
export declare class SDKClient implements Client {
    private httpClient;
    private baseUrl;
    /**
     * Creates a new SDK client instance with the specified configuration.
     *
     * @param config - Configuration object with API key and optional settings
     */
    constructor(config: SDKConfig);
    /**
     * @inheritDoc
     */
    run(params: RunParams): Promise<AttestationResult>;
    /**
     * @inheritDoc
     */
    verifyAttestation(quoteData: AttestationQuoteData): Promise<AttestationVerificationResult>;
    /**
     * @inheritDoc
     */
    postToDataAvailability(data: any, namespace?: string): Promise<DataAvailabilityResult>;
    /**
     * @inheritDoc
     */
    getFromCelestia(params: CelestiaGetParams): Promise<CelestiaGetResult>;
}
